name: Ejercicio 10 - Aprobación Manual y Despliegue

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    name: "Build Application"
    runs-on: ubuntu-latest
    steps:
      - run: echo "Compilando y preparando artefactos..."

  deploy:
    name: "Deploy to Production"
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'workflow_dispatch'
    steps:
      - name: Deploy Step
        run: echo "Desplegando en producción tras aprobación manual..."